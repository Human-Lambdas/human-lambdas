import styled from '@emotion/styled'

const PageWrapper = styled.div({
  height: '100%',
  maxWidth: '100%',
  padding: '50px 35px 0',
  overflow: 'auto',
  backgroundColor: '#fff'
})

export default PageWrapper
