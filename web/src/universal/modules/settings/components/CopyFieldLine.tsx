import styled from '@emotion/styled'

const CopyFieldLine = styled.div({
  display: 'grid',
  gridTemplateColumns: '1fr 0px',
  alignItems: 'center',
  width: '100%',
  position: 'relative',
  marginBottom: 15
})

export default CopyFieldLine
